-- =========================================================================
-- RainonUI / MDTTooltip: наш блок в подсказке Mythic Dungeon Tools.
--
-- ЧТО ДЕЛАЕТ: когда игрок наводит курсор на противника на карте MDT, MDT
-- показывает свою модель-подсказку (глобальный фрейм "MDTModelTooltip") и
-- заполняет её моделью моба: tooltip.Model:SetCreature(npcID). Мы вешаем
-- пост-хук на SetCreature, ловим npcID и, если он есть в NOTES, дорисовываем
-- снизу свою маленькую рамку с заметкой.
--
-- ПОЧЕМУ ТАК (а не hooksecurefunc(MDT, "DisplayBlipTooltip")): в текущей MDT
-- (ветка Midnight) НЕТ глобала _G.MDT — внутренняя таблица приватная, наружу
-- отдаётся только proxy MythicDungeonToolsAPI без DisplayBlipTooltip. Поэтому
-- цепляемся за то, что MDT называет ГЛОБАЛЬНО: фрейм "MDTModelTooltip" и его
-- .Model:SetCreature(npcID). Это стабильный публичный якорь.
--
-- БЕЗОПАСНОСТЬ:
--  * фреймы MDT НЕ трогаем — только hooksecurefunc/HookScript (пост-хуки);
--  * если MDT не установлен / окно ни разу не открывали — модуль просто спит;
--  * npcID проверяем на Secret Value (issecretvalue) и tonumber;
--  * это ЭКСПЕРИМЕНТАЛЬНЫЙ модуль — строку в .toc можно закомментировать (#),
--    и остальной RainonUI продолжит работать как ни в чём не бывало.
-- =========================================================================
local ADDON_NAME, ns = ...

-- -------------------------------------------------------------------------
-- NOTES: our per-mob notes, keyed by npcID -> "text".
-- Matched purely by npcID. Dungeon headers below are COMMENTS ONLY (they
-- don't affect behaviour): they exist so an edit like "add a note to
-- Гробница Королей" is one Ctrl+F on "DUNGEON: <ru name>". Source of entries:
-- the per-dungeon Obsidian page linked from _INDEX/MDT_TOOLTIP.md
-- (Имя | NPC_ID | Текст) — copy NPC_ID + text here. Note text stays RU
-- (shown in-game); code comments are EN.
-- -------------------------------------------------------------------------
local NOTES = {
    -- == DUNGEON: Алтарь Клыков ==
    [261557] = "Сбиваем [Эволюцию] с помощью [Слепящий свет] или [Молот правосудия], таурены могут использовать [Громовая поступь].", -- Верховный мастер эволюции
    [261554] = "Снимаем замедление с помощью [Благословенная свобода].", -- Двуглавый кусатель
    [261560] = "Обязательно сбиваем [Пронзительное шипение].", -- Изначальный змей
    [259445] = "Танк держит у кучек, где нет трупа дракона.", -- Рав'и
    -- == DUNGEON: Арена Шрама Бездны ==
    [238883] = "Обязательно сбиваем - Деморализующий крик.", -- Подчиненный буян
    [238887] = "Водим босса по кругу вдоль стенок.", -- Таз'ра
    [239008] = "Танк подводит к колодцу босса.\nТанк кайтит маленького моба из колодца.", -- Атрокс
    [241496] = "Убиваем - Тотем магмы в первую очередь!", -- Порабощенный шаман
    [243766] = "Обязательно сбиваем - Безумный визг - это масс фир", -- Килижор-крикун
    [244309] = "Танк направляет в стенку, стоит у стенки.", -- Бруток
    [244708] = "Обязательно сбиваем - Исцеляющая Бездна", -- Смотритель Бездны
    [249590] = "Сбиваем - Жестокие пески. Если не сбили - жмём Фридом.", -- Злобный кролуск
    [252053] = "Как можно быстрее пробиваем щит, чтобы перестал аоешить.", -- Жестокий надзиратель
    [267545] = "Быстро убиваем Копьё, чтобы выйти из аое урона!\nДаём ФизБоП в дебафф прыжка.", -- Эгира Непреклонная
    [268184] = "Поглощает и отхиливается моба в красном столбе. Добиваем до того, как сожрёт.", -- Всепожирающий изверг
    -- == DUNGEON: Гробница Королей ==
    [269810] = "Убиваем тотемы в порядке - Огненный > Громовой > Потока", -- Заназал Мудрый
    [135167] = "На танке снимаем стаки [Кровожадный топор] с помощью [Божественный щит] или [Благословение защиты].", -- Королевский берсерк
    [136160] = "Копья можно игнорировать с помощью [Благословение защиты].", -- Король Дазар
    [133935] = "Просим снимать энрейдж, иначе танку будет очень больно.", -- Оживший страж
    [135239] = "Убиваем тотем хила в приоритете.\nСнимаем [Ледяной шок] с помощью [Благословенная свобода].", -- Призрачная шаманка
    [133943] = "Эльфы крови должны пуржить.", -- Прислужник Зула
    [138493] = "Эльфы крови должны пуржить.", -- Прислужник Зула
    [135204] = "Сбиваем [Сглаз], чтобы союзник не попал в контроль.", -- Фантомный жрец-проклинатель
    -- == DUNGEON: Закоулок Душегубов ==
    [235268] = "Сбиваем [Канал здоровья] с помощью [Слепящий свет] или [Молот правосудия], таурены могут использовать [Громовая поступь].", -- Заклинатель Скверны
    [234647] = "Топор кладём ближе к боссу.\nТанк водит босса по краю круга.", -- Затуукс Разрушитель
    [234649] = "Поджигаем красным кругом ТОЛЬКО ЗЕЛЁНУЮ бочку!\nСнимаем яд с танка с помощью [Очищение от токсинов].", -- Заэн Траурный Клинок
    [234763] = "Танк не заводит голема в портал!", -- Литиэль Пепельная Ярость
    [236905] = "Танк отворачивает лицом в стенку, чтобы заспавнить луч. Из луча можно выходить.", -- Мастер Скверны Люксей
    [236071] = "[Удар щитом] наносит очень большой урон по танку + накладывает дебафф.\nАура защиты и урона для союзников.", -- Подкупленный стражник
    [235265] = "УБИВАЕМ КАК МОЖНО БЫСТРЕЕ!", -- Порабощенный чернокнижник
    [235267] = "Сбиваем [Ярость Скверны] - снижение получаемого урона.", -- Страж гнева – живодер
    -- == DUNGEON: Рубиновые Омуты ==
    [197698] = "Кидаем МБоП или Сакру человеку с дебаффом.", -- Громоголов
    [187897] = "Танк отворачивает чардж в стенку.", -- Драгхар Отрицатель
    [187894] = "Снимаем его дебафф фридомом, чтобы не замерзнуть.", -- Заряженный дракончик
    [189893] = "Снимаем его дебафф фридомом, чтобы не замерзнуть.", -- Заряженный дракончик
    [197509] = "Можно спуржить щит.", -- Изначальная грозовая туча
    [188244] = "Очень больно долбит танка.", -- Изначальный исполин
    [190206] = "Обязательно сбиваем с помощью стана или блайнда - Поджигающий обстрел", -- Пеплозрящий огнехлестатель
    [190034] = "Обязательно сбиваем - Горячая вспышка.", -- Пламенный разрушитель
    [188067] = "|T236209:0|t - Сбиваем щит", -- Ткач Холода Морозной Вспышки
    -- == DUNGEON: Храм Сетралисс ==
    [133392] = "Закидываем [Возложение рук], а также хилим в свободное время.", -- Аватара Сетралисс
    [136076] = "Эльф крови может снять [Накопление заряда] расовой способностью.", -- Беспокойное облако
    [263658] = "Танк отводит к стене. После аое урона выводим уз лужи, ведём вдоль стенки.\nЕсли танк сокает, получает больше урона от босса.", -- Гальваззт
    [262530] = "Синий круг - делим.\nБольшой белый круг - выбегаем.", -- Гюрзис
    [268344] = "Выходим из большого фиолетового круга.\nПОСЛЕ КРУГА СРАЗУ СОКАЕМ маленькие круги.", -- Зараженный порчей страж
    [133384] = "Сбиваем [Клубок змей] с помощью [Слепящий свет] или [Молот правосудия], таурены могут использовать [Громовая поступь].", -- Меректа
    [134364] = "Сбиваем [Помутнение разума], поддерживаемый дезориент игрока.", -- Отступник-подчинитель
    [134600] = "Сбиваем [Залп стрел] с помощью [Слепящий свет] или [Молот правосудия], таурены могут использовать [Громовая поступь].\nДиспелим яд с помощью [Очищение от токсинов].", -- Песчаный охотник
    -- == DUNGEON: Слепящая Долина ==
    -- == DUNGEON: Берлога Налоракка ==
    [241869] = "|T236209:0|t - Можно снять фридомом", -- Аватара решимости
    [245855] = "|T2101983:0|t - Убиваем его тотем в приоритете", -- Дух голода
    [241814] = "|T135863:0|t - Исцеляет, сбиваем!", -- Землевещун-хранитель
    [241911] = "Пробиваем щит, чтобы элем перестал аоешить по группе.", -- Землистый грохотун
    [241872] = "|T7263309:0|t - Сбиваем, чтобы не получить замедление.", -- Морозный терзатель
    [241812] = "Идём по кучкам - Мясо > Кости > Грибы", -- Прозапасник
    [241808] = "После смерти маленьких медведей впадает в ярость. Можно снять.", -- Территориальный матриарх
}

-- -------------------------------------------------------------------------
-- Наша рамка (создаём лениво, привязываем к тултипу MDT)
-- -------------------------------------------------------------------------
local block

local function EnsureBlock(mdtTip)
    if block then return block end
    if not mdtTip then return nil end

    block = CreateFrame("Frame", "RainonUI_MDTNoteBlock", mdtTip, "TooltipBorderedFrameTemplate")
    block:SetFrameStrata("TOOLTIP")
    block:SetFrameLevel((mdtTip:GetFrameLevel() or 1) + 10)
    block:SetClampedToScreen(true)
    block:Hide()

    -- Приклеиваем ПОД подсказку MDT, слева по краю, с небольшим зазором.
    block:ClearAllPoints()
    block:SetPoint("TOPLEFT", mdtTip, "BOTTOMLEFT", 0, -2)

    block.caption = block:CreateFontString(nil, "OVERLAY", "GameFontNormalSmall")
    block.caption:SetPoint("TOPLEFT", block, "TOPLEFT", 10, -8)
    block.caption:SetJustifyH("LEFT")
    block.caption:SetTextColor(0.4, 0.7, 0.55, 1)
    block.caption:SetText("RainonUI")

    block.text = block:CreateFontString(nil, "OVERLAY", "GameFontNormalSmall")
    block.text:SetPoint("TOPLEFT", block.caption, "BOTTOMLEFT", 0, -4)
    block.text:SetJustifyH("LEFT")
    block.text:SetTextColor(1, 0.9, 0.5, 1)
    block.text:SetWidth(270)

    return block
end

-- Высота рамки по факту текста. GetStringHeight у ещё не отрисованной строки
-- может вернуть 0 (а 0 в Lua истинно, поэтому `... or 12` не спасает) —
-- поэтому явная защита от нуля.
local function SizeBlock(b)
    local capH = b.caption:GetStringHeight(); if not capH or capH < 1 then capH = 12 end
    local txtH = b.text:GetStringHeight();    if not txtH or txtH < 1 then txtH = 14 end
    b:SetWidth(290)
    b:SetHeight(8 + capH + 4 + txtH + 10)
end

local function ShowNote(mdtTip, note)
    local b = EnsureBlock(mdtTip)
    if not b then return end
    b.text:SetText(note)
    SizeBlock(b)
    b:Show()
    -- На ПЕРВОМ показе строки ещё не отмерены — пересчитываем размер на
    -- следующем кадре, когда высота текста уже известна.
    if C_Timer and C_Timer.After then
        C_Timer.After(0, function() if b:IsShown() then SizeBlock(b) end end)
    end
end

local function HideNote()
    if block then block:Hide() end
end

-- Модуль включён? Галка «ВКЛ модуль» на вкладке «Подземелья»
-- (features.dungeonModule). По умолчанию (nil) — включён.
local function Enabled()
    return not (ns.db and ns.db.features and ns.db.features.dungeonModule == false)
end

-- -------------------------------------------------------------------------
-- Установка хука на модель-тултип MDT (глобальный фрейм "MDTModelTooltip").
-- Ставится один раз, как только фрейм существует.
-- -------------------------------------------------------------------------
local hooked = false

local function InstallTooltipHook()
    if hooked then return true end
    local tip = _G.MDTModelTooltip
    if not (tip and tip.Model and tip.Model.SetCreature) then return false end

    -- MDT на наведении зовёт tip.Model:SetCreature(npcID) — ловим npcID здесь.
    hooksecurefunc(tip.Model, "SetCreature", function(_, creatureID)
        if not Enabled() then HideNote(); return end
        -- Всё тело в защите: npcID может прийти как Secret Value (12.0).
        local ok = pcall(function()
            if issecretvalue and issecretvalue(creatureID) then HideNote(); return end
            local id = tonumber(creatureID)
            local note = id and NOTES[id]
            if note then ShowNote(tip, note) else HideNote() end
        end)
        if not ok then HideNote() end
    end)

    -- Тултип спрятался — прячем и наш блок.
    tip:HookScript("OnHide", HideNote)

    hooked = true
    if ns.debug and ns.Print then ns.Print("MDT-подсказка: хук на MDTModelTooltip установлен.") end
    return true
end

-- -------------------------------------------------------------------------
-- Запуск. Фрейм "MDTModelTooltip" MDT строит ЛЕНИВО — только при первом
-- открытии окна MDT, и это может случиться в ЛЮБОЙ момент сессии.
--
-- Поэтому таймер работает БЕЗ лимита по времени и сам гасится в тот же миг,
-- как хук поставлен. Раньше стоял предел ~60 сек после каждого входа в мир:
-- если игрок открывал MDT позже этого окна, фрейм появлялся уже после того,
-- как таймер сдался, и заметки не грузились всю сессию (до /reload или смены
-- зоны, где окно проб открывалось заново). Стоимость опроса — один поиск
-- глобала раз в 2 сек, только пока хук не встал; после — тикер выключен.
--
-- Тикер один (без дублей на каждый loading screen) и не запускается, если
-- аддон MythicDungeonTools не загружен (тогда фрейма не будет вовсе).
-- -------------------------------------------------------------------------
local installTicker

local function MDTLoaded()
    if C_AddOns and C_AddOns.IsAddOnLoaded then
        return C_AddOns.IsAddOnLoaded("MythicDungeonTools")
    end
    return true -- не можем проверить — не мешаем попыткам
end

local function StartInstall()
    if not Enabled() then return end
    if InstallTooltipHook() then return end
    if installTicker or not MDTLoaded() then return end
    if not C_Timer or not C_Timer.NewTicker then return end
    installTicker = C_Timer.NewTicker(2, function()
        if InstallTooltipHook() then
            if installTicker then installTicker:Cancel(); installTicker = nil end
        end
    end)
end

ns.RegisterEvent("PLAYER_ENTERING_WORLD", function()
    if not hooked then StartInstall() end
end)

-- Публичное: галка «ВКЛ модуль» дёргает Refresh при переключении.
-- Вкл — пробуем поставить хук; выкл — прячем заметку (хук, если уже стоит,
-- сам проверит Enabled() и ничего не покажет).
ns.MDTNotes = {
    Refresh = function()
        if Enabled() then StartInstall() else HideNote() end
    end,
}
